# awesome-x402 submission

Add to `github.com/xpaysh/awesome-x402` (and optionally `Merit-Systems/awesome-x402`)
under **Example Applications → API Examples** (or nearest equivalent section).

## Entry line

```markdown
- [WebLens](https://api.weblens.dev) — Premium web intelligence API. 18 paid endpoints for scraping (static + JS render), search, structured/NL extraction, PDF, batch, research, monitoring, and intel. USDC on Base via PayAI+CDP facilitators. Also ships an MCP server ([`@weblens/mcp`](https://www.npmjs.com/package/@weblens/mcp)) and Coinbase AgentKit provider. Discovery: [/.well-known/x402](https://api.weblens.dev/.well-known/x402).
```

## PR description

```
Add WebLens to API Examples

WebLens is an x402-monetized web intelligence API on Cloudflare Workers,
live at api.weblens.dev since late 2025. 18 paid endpoints priced $0.005–$8,
USDC on Base, PayAI primary + CDP fallback facilitators. Also available
as MCP server and AgentKit provider.
```
